x make down key not carry over to next piece
x show preview of next piece
x add side attempt when rotating next to edge
x add start screen
x add game over screen (retry)
x add pause button
x add pause overlay
  add README
  add LICENSE
  add difficulty? maybe faster ticks, junk rows
  play around with color
    each piece has a color
    desaturate colors when they are added to the grid
    make ghost a desaturated tint of current piece color
